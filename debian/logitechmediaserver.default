# Defaults for Logitech Media Server initscript
# sourced by /etc/init.d/logitechmediaserver
# installed at /etc/default/logitechmediaserver by the maintainer scripts

#
# This is a POSIX shell fragment
#

# User to run Logitech Media Server as
SLIMUSER=squeezeboxserver

#Locations of preferences, cache and logs
PREFSDIR=/var/lib/$SLIMUSER/prefs
LOGDIR=/var/log/$SLIMUSER/
CACHEDIR=/var/lib/$SLIMUSER/cache

# Additional options that are passed to the daemon.
SLIMOPTIONS=""
